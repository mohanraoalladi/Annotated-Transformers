Below are commands to run this project
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python -m spacy download de_core_news_sm
python -m spacy download en_core_web_sm
python main.py

caffeinate -d python main.py
caffeinate -d python train_main.py
caffeinate -d python evaluate.py


git init
git add .
git commit -m "Initial commit - Modular Transformer implementation"

git branch -M main
git remote add origin https://github.com/mohanraoalladi/Annotated-Transformers.git

git pull origin main --allow-unrelated-histories

git push -u origin main
git push -u origin main --force



git add .
git commit -m "updated logic to train for save after each epoch"
git push -u origin main --force



git rm -r --cached venv
git add .gitignore
git add .
git commit -m "Fix .gitignore and remove venv from Git tracking"
git push -u origin main --force

